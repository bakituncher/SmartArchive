<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.CepArsivi" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
    </style>

    <style name="Theme.CepArsivi" parent="Base.Theme.CepArsivi" />

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- Açılış Ekranı (Splash Screen) Teması -->
    <!-- Açılış Ekranı (Splash Screen) Teması -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- DÜZELTME: Arka plan rengi, logonun rengiyle uyumlu hale getirildi -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Ekranda gösterilecek logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/codenzi</item>

        <!-- Animasyonun ne kadar süreceği (milisaniye) -->
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <!-- Açılış ekranı bittikten sonra uygulanacak asıl tema -->
        <item name="postSplashScreenTheme">@style/Theme.CepArsivi</item>
    </style>


</resources>